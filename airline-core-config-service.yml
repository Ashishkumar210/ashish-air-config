server:
  port: 5002

spring:


  datasource:
    url: jdbc:mysql://localhost:3306/airline
    username: root
    password: Ashish@2100

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  cloud:
    config:
      enabled: false

# ✅ OUTSIDE spring (IMPORTANT)
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  #    service-url:
  #      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true